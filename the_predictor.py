# input every time-stamp with all coresponding values
# split in 5s intervalls for the last 15min
# do time series prediction to predict the final time (basically AI) 
class the_predictor:
    path_to_weights = "/weights.wgh"
    path_to_data = "/data.idk"
    current_subjects = [] # will be [[[worldtime,arrival_time],...],...]
    current_subject_numbers = []
    def add_time_stamp(time, subjects): # subjects = [[nr, time],...]

    def finalize_subject(nr): # generate viable data

    def 
